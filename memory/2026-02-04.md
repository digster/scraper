# 2026-02-04 - MCP Feature Parity Fix

## Summary
Fixed feature parity gaps in the MCP interface by adding two missing parameters to `scraper_start` tool.

## Changes Made

### 1. Added `stateFile` parameter to MCP
- **File**: `internal/mcp/types.go`
- Added `StateFile string` field to `StartCrawlInput` struct
- Allows crawls to be resumed via MCP (was only available in CLI, API, GUI)

### 2. Added `verbose` parameter to MCP
- **File**: `internal/mcp/types.go`
- Added `Verbose bool` field to `StartCrawlInput` struct
- Enables debug output when crawling via MCP

### 3. Updated tools.go to parse new parameters
- **File**: `internal/mcp/tools.go`
- Added parsing logic in `handleStart()` function for both `stateFile` and `verbose`
- Follows existing pattern: type assertion → assignment to `crawlReq`

### 4. Updated skill documentation
- **File**: `docs/skill.md`
- Added `stateFile` and `verbose` to the MCP scraper_start parameters table

## Feature Parity Status
All four interfaces now support the same 35 configuration parameters:
- CLI: 35 flags
- MCP: 35 params (was 33, now complete)
- HTTP API: 35 fields
- GUI: 35 fields

## Files Modified
- `internal/mcp/types.go`
- `internal/mcp/tools.go`
- `docs/skill.md`
